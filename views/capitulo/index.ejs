<script type="text/javascript">
    const atividade = parseInt("<%= atividade %>");
    const livro = parseInt("<%= livro %>");

    function enviarResposta() {

    }
</script>
<%
	let cores = [
		"secao1",
		"secao2",
		"secao3",
		"secao4",
		"secao5"
	];
%>

<%let i;
for(let item of liberadas){
    if(item.idatividade == atividade){
        i = liberadas.indexOf(item)
    }
}%>
<div class="container-fluid c">
    <div style="text-align: left; margin-top: 1em;">
        <a class="middle" href="/notas"><i class="fas fa-arrow-left fa-2x"></i></a>
        <h2 class="text-center titulo middle"><%=liberadas[i].nomecapitulo%></h2>    
    </div>
        <div class="row">
            <div class="col-sm-9 bg-1">
                <%- include("livro" + livro + "/atividade" + atividade)%>
            <div class="pergunta mt-3">
                <button style="font-size:0.8em;" id="proximo" disabled type="button" class="btn btn-success textura x2"> <i class="fas fa-long-arrow-alt-right"></i> PRÓXIMO</button>
            </div>
            </div>
            <div class="col-sm-3 lista-secoes">
                <%for(let i = 0; i < secoes.length; i++){%>
                    <a class="secao <%-cores[i]%>" href='<%staticRoot%>/capitulo/atividade/<%=i+1%>/<%=lista.idlivro%>'>
                        <img class="icone-menu" src="/public/img/icones/icone<%-(i+1)%>.png" alt=""/><%=secoes[i].nome%>
                    </a>
                <%}%>
            </div>
        </div>
</div>

<%- contentFor("styles") %>
<style>

    .textura{
        background-image: url('/public/img/textura.png');
    }

    .icone-menu{
        width: 30%;
        max-width: 100px;
        margin: -0.75em 0.5em -0.75em -0.5em;
    }

    .secao{
        font-family: 'Yomogi', cursive;
        background-image: url('/public/img/textura.png');
        font-size: 2em;
        padding: 0.75em !important;
        display: block;
        width: 100%;
        border-radius: 0.75em;
        margin-bottom: 2.5em;
        transform: translate(0px, 0px);
        transition: box-shadow ease-in-out 0.3s, transform ease-in-out 0.3s;
    }

    a:hover{
        color: white;
    }

    .color-white{
        color: white;
    }

    .c{
        background-color: rgba(241, 241, 241, 0.711);
    }

    .titulo{
        font-family: 'Annie Use Your Telescope', cursive;
        text-transform: uppercase;
        font-size: 4em;
        font-weight: 300;
        padding: 30px;
        display: inline-block; 
        width: 90%;
    }

</style>